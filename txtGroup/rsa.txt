*보안세미나_해킹방법

1.7

Md5 : message-digest algorithm5 (128비트 암호화 해시 함수)
 크래킹

1. 사전 대입 공격 : 사용자가 설정하는 대부분의 패스워드에 특정 패턴이 있음
	>> 개인 신상정보 유출 시 매우 효율적인 공격방법
2. 무작위 대입 공격(brute force attack) : 위 사전대입공격 무산 시 주로 사용, 패스워드 문자열의 범위를 정하고,
	범위 내 모든 문자열을 대입 >> 패스워드가 복잡하지 않거나 짧은 경우 효율적
3. 레인보우 테이블 공격 : 패스워드 HASH를 연결된 Chain으로 저장한 대규모 테이블 (패스워드 HASH를 미리 만듬)
	숫자 (12자리), 소문자(10자리), 숫자+소문자(8자리), 숫자+소문자+대문자(7자리)

DNS Spoofing : DNS 서버 자체를 감염
			2) 장비 내 DNS 테이블을 위조

SSL/TLS

: SSL 인증서는 클라이언트와 서버간의 통신을 제3자가 보증해주는 전자화된 문서다. 
클라이언트가 서버에 접속한 직후에 서버는 클라이언트에게 이 인증서 정보를 전달한다.
클라이언트는 이 인증서 정보가 신뢰할 수 있는 것인지를 검증 한 후에 다음 절차를 수행하게 된다.
SSL과 SSL 디지털 인증서를 이용했을 때의 이점은 아래와 같다.
1. 통신 내용이 공격자에게 노출되는 것을 막을 수 있다.
2. 클라이언트가 접속하려는 서버가 신뢰할 수 있는 서버인지를 판단할 수 있다.
3. 통신 내용의 악의적인 변경을 막을 수 있다.


가장 중요한 기능 
1. 클라이언트가 접속한 서버가 신뢰 할 수 있는 서버임을 보장한다.
2. SSL 통신에 사용할 공개키를 클라이언트에게 제공한다.


CA
인증서의 역할은 클라이언트가 접속한 서버가 클라이언트가 의도한 서버가 맞는지를 보장하는 역할


SSL의 취약점 
1. 악성사이트인지 판단해주지 않는다.
2. 공격자들도 사용가능
3. 현재 매우 약한 SHA-1기반

SSL과 TLS 차이점

무결성(Integrity) : 허가되지 않은 정보의 수정을 검출하는 것을 의미하며, 흔히 메시지의 변경이나 위조에 대한 대처 방안을 말합니다.
둘다 무결성을 보장해준다.

SSL은 birthday attack이 같다

*RSA-OAEP


